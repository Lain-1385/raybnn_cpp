#include "dataloader/dataloader.hpp"
#include <catch2/catch_all.hpp>
#include <catch2/catch_test_macros.hpp>

/*
TEST_CASE("test_loadcsv2tensor", "[load_csv2tensor]") {
    // Test loading a CSV file into a tensor
    std::string file_path = "path/to/your/csvfile.csv";
    torch::Tensor tensor = load_csv2tensor(file_path);
    REQUIRE(tensor.defined());
    REQUIRE(tensor.dim() == 2);
}
    */